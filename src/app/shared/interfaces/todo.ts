//This is the interface for the todo object. It has two properties, title and description, both of which are strings. This interface will be used to define the structure of the todo object. The todo object will be used to store the title and description of a todo item.
export interface Todo {
  title: string;
  description: string;
}
